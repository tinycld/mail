#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "Generating addon wiring..."
npm run addons:generate

cd server

echo "Building PocketBase..."
go build -o tinycld .
echo "Starting PocketBase..."
exec ./tinycld --dev serve "$@"
